# Django
DJANGO_SETTINGS_MODULE=chamaplus_backend.settings.development
SECRET_KEY=change-me-to-a-long-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL (XAMPP defaults — or remote Aiven host/port/user/password)
DB_ENGINE=django.db.backends.mysql
DB_NAME=chamaplus_db
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
# For Aiven / managed MySQL (leave empty for local XAMPP):
# DB_SSL_MODE=REQUIRED
# DB_SSL_CA=certs/aiven-ca.pem

# CORS (comma-separated origins for production)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Media / Static
MEDIA_URL=/media/
STATIC_URL=/static/
